# Production Environment Variables for Railway
ENVIRONMENT=production
HOST=0.0.0.0
PORT=${PORT:-5005}

# Security related
API_PREFIX=${API_PREFIX:-}
AUTHORIZATION=${AUTHORIZATION:-}
AUTH_KEY=${AUTH_KEY:-}

# Request related
CHATGPT_BASE_URL=${CHATGPT_BASE_URL:-https://chatgpt.com}
PROXY_URL=${PROXY_URL:-}
EXPORT_PROXY_URL=${EXPORT_PROXY_URL:-}
IMPERSONATE=${IMPERSONATE:-[]}
USER_AGENTS=${USER_AGENTS:-[]}

# Function related
HISTORY_DISABLED=${HISTORY_DISABLED:-true}
POW_DIFFICULTY=${POW_DIFFICULTY:-000032}
RETRY_TIMES=${RETRY_TIMES:-3}
CONVERSATION_ONLY=${CONVERSATION_ONLY:-false}
ENABLE_LIMIT=${ENABLE_LIMIT:-false}
UPLOAD_BY_URL=${UPLOAD_BY_URL:-false}
CHECK_MODEL=${CHECK_MODEL:-false}
SCHEDULED_REFRESH=${SCHEDULED_REFRESH:-false}
RANDOM_TOKEN=${RANDOM_TOKEN:-true}
OAI_LANGUAGE=${OAI_LANGUAGE:-en-US}

# Gateway related
ENABLE_GATEWAY=${ENABLE_GATEWAY:-false}
AUTO_SEED=${AUTO_SEED:-false}

# Railway specific
ACTIVE_TOKEN=${ACTIVE_TOKEN:-aziz}